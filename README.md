# RAG-Assistente-Corporativo

# Projeto 1 - Assistente Corporativo de Perguntas e Respostas com RAG Sobre Documentos Internos

# Abra o terminal ou prompt de comando, navegue até a pasta com os arquivos e execute o comando abaixo para criar um ambiente virtual:

conda create --name dsamebvp1 python=3.13

# Ative o ambiente:

conda activate dsamebvp1 (ou: source activate dsamebvp1)

# Instale o pip e as dependências:

conda install pip
pip install -r requirements.txt 

# Execute o script:

streamlit run dsa_app.py

# Acesse o jupyter pelo seu navegador padrão.

# Para testar a app:

# Qual a antecedência mínima para solicitação de férias?
# Há necessidade de presença física em algum momento no modelo de trabalho remoto?
# Posso instalar softwares por conta própria no computador da empresa?

# Use os comandos abaixo para desativar o ambiente virtual e remover o ambiente (opcional):

conda deactivate
conda remove --name dsamebvp1 --all
